{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "wait-for-mparser",
            "type": "shell",
            "command": "node",
            "args": [
                "-e",
                "const http=require('http');function checkServer(){http.get('http://localhost:9002',r=>{if(r.statusCode===200)process.exit(0);else setTimeout(checkServer,1000)}).on('error',()=>setTimeout(checkServer,1000))}checkServer()"
            ],
            "presentation": {
                "reveal": "never"
            }
        },
        {
            "label": "delay-start",
            "type": "shell",
            "command": "sleep",
            "args": ["2"],
            "windows": {
                "command": "timeout",
                "args": ["/t", "2"]
            },
            "presentation": {
                "reveal": "never"
            }
        },
        {
            "label": "prepare-launch",
            "type": "shell",
            "command": "echo",
            "args": ["准备启动所有服务..."],
            "presentation": {
                "reveal": "always"
            }
        }
    ]
} 